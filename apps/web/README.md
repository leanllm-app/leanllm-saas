# Your Application

Write here everything about your application.

## Icones

Este app usa a lib [`@solar-icons/react`](https://solar-icons.vercel.app/docs/packages/react).

Para evitar problemas de SSR em componentes server (Next.js App Router), importe os icones pela entrada SSR:

```tsx
import { Key } from '@solar-icons/react/ssr';
```
